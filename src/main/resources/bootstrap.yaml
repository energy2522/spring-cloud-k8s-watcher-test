spring:
  application:
    name: config-server
  cloud:
    kubernetes:
      config:
        sources:
          - name: k8s-config-server
          - name: ${spring.application.name}
management:
  endpoints:
    web:
      exposure:
        include: refresh
